{% extends 'base.html.twig' %}

{% block title %}Vidéos{% endblock %}

{% block body %}

<div class="video_container">
    {% for video in video %}
    	<div class="video">
    		<h3>{{ video.title }}</h3>
    		<iframe width="1912" height="768" src="https://www.youtube.com/embed/{{ video.url|slice(32, 11) }}" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
    	</div>
        <hr>
    {% endfor %}
</div>
{% endblock %}
